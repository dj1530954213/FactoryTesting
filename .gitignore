# ===========================================
# Tauri + Angular 项目 .gitignore 文件
# ===========================================

# Node.js 相关
# ============
node_modules/                    # npm 依赖包
npm-debug.log*                   # npm 调试日志
yarn-debug.log*                  # yarn 调试日志
yarn-error.log*                  # yarn 错误日志
lerna-debug.log*                 # lerna 调试日志
.npm                            # npm 缓存目录
.yarn-integrity                 # yarn 完整性检查

# Angular 相关
# =============
/dist/                          # Angular 构建输出
/tmp/                           # 临时文件
/out-tsc/                       # TypeScript 编译输出
.angular/                       # Angular 缓存
/bazel-out                      # Bazel 输出

# Tauri 相关
# ===========
/src-tauri/target/              # Rust 编译输出
/src-tauri/WixTools             # Windows 安装程序工具

# Rust 相关
# ==========
target/                         # Cargo 构建输出
debug/                          # 调试版本
**/*.rs.bk                      # rustfmt 备份文件
*.pdb                           # Windows 调试信息
Cargo.lock                      # 依赖锁文件 (通常应该提交，但可根据需要调整)

# 编译输出和缓存
# ==============
*.log                           # 日志文件
*.cache                         # 缓存文件
.cache/                         # 缓存目录

# 编辑器和 IDE
# =============
.vscode/                        # VS Code 设置 (可选择性忽略)
.idea/                          # JetBrains IDE 设置
*.swp                           # Vim 交换文件
*.swo                           # Vim 交换文件
*~                              # 备份文件
.project                        # Eclipse 项目文件
.classpath                      # Eclipse 类路径
.c9revisions                    # Cloud9 修订
.settings/                      # Eclipse 设置

# 操作系统相关
# ============
.DS_Store                       # macOS 文件夹属性
.DS_Store?                      # macOS 文件夹属性
._*                             # macOS 资源分支
.Spotlight-V100                 # macOS Spotlight 索引
.Trashes                        # macOS 垃圾箱
ehthumbs.db                     # Windows 缩略图缓存
Thumbs.db                       # Windows 缩略图缓存
Desktop.ini                     # Windows 文件夹配置

# 环境变量和配置
# ==============
.env                            # 环境变量文件
.env.local                      # 本地环境变量
.env.development.local          # 开发环境变量
.env.test.local                 # 测试环境变量
.env.production.local           # 生产环境变量
.env.*.local                    # 其他本地环境变量

# 密钥和证书
# ==========
*.key                           # 私钥文件
*.pem                           # 证书文件
*.p12                           # 证书文件
*.pfx                           # 证书文件

# 测试相关
# ========
coverage/                       # 测试覆盖率报告
*.lcov                          # 覆盖率数据文件
.nyc_output                     # NYC 测试覆盖率

# 临时文件
# ========
*.tmp                           # 临时文件
*.temp                          # 临时文件
*.bak                           # 备份文件
*.old                           # 旧文件

# 打包和分发
# ==========
*.tgz                           # 压缩包
*.tar.gz                        # 压缩包
dist.zip                        # 分发包

# 文档生成
# ========
docs/                           # 生成的文档 (可选择性忽略)

# 数据库文件
# ==========
*.sqlite                        # SQLite 数据库文件
*.sqlite3                       # SQLite3 数据库文件
*.db                           # 通用数据库文件
FactoryTesting/src-tauri/data/factory_testing_data.sqlite  # 应用数据库文件
FactoryTesting/src-tauri/data/*.sqlite                     # 所有数据目录下的SQLite文件

# 其他常见忽略项
# ==============
.eslintcache                    # ESLint 缓存
.stylelintcache                 # Stylelint 缓存
*.pid                           # 进程 ID 文件
*.seed                          # 随机种子文件
*.pid.lock                      # 进程锁文件